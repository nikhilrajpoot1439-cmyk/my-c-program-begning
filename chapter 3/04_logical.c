#include<stdio.h>

int main(){
    // int a=1;int b=1;
    int a=0;int b=1;
    printf("The value of a and b is %d" ,a&&b);

    //&& for perform AND operation

    printf("\nThe value of a or b is %d" , a||b);

     //     ||  is used for perform OR operation
printf("\nThe value of not of a is %d" , !a);


     //   ! is used to perform NOT operation

return 0;
}